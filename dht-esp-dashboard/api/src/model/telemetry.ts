export type Telemetry = {
    deviceId: string,
    temperature: number;
    humidity: number;
    timestamp: number;
}

export type Telemetry_MQTT = {
    device_id: string,
    temperature: number;
    humidity: number;
    timestamp: number;
}